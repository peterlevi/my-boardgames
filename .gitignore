# Real credentials — never commit
credentials.env

# A BGG collection CSV export: it carries what you paid for each game
data/collection.csv

# Python
__pycache__/
*.pyc

# Full-size thumbnail originals — regenerable, the downscaled jpgs are kept
data/thumbs/orig/
data/thumbs/_fetch.conf

# Generated reports
reports/

# CI build output
site/

# README screenshots: rendered by CI into the published site, never committed
docs/

# Machine-specific agent confinement (absolute paths, this machine only)
.claude/settings.local.json
