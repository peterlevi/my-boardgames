# Optional corrections to the derived scoring-breadth level.
#
# This file is EMPTY on purpose. Breadth is computed in scripts/breadth.py from
# counted evidence in BGG's mechanic and family tags — no game is named in the
# model. Use this only where those tags genuinely cannot see what a game does,
# and give a reason, so the next person can tell a real blind spot from a
# preference.
#
# Format:  Game name = Focused | Some | Broad | Salad
#
# Known blind spots, left uncorrected so the model stays honest:
#   Pulsar 2849       computes Focused; its many end-game scoring categories
#                     have no corresponding BGG tag.
#   Food Chain Magnate computes Broad; its private signals outnumber the single
#                     shared one, though in play the market is the whole game.
